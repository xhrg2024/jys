辑佚史智能体构建方案
基础信息
项目	内容
基础模型	Qwen2.5-7B-Instruct
核心数据	`data.json`（原始辑佚史实体与关系数据）
问答数据	`sft_alpaca(1).json`（134条，Alpaca格式，需回填答案）
工具调用数据	`sharegpt_toolcall(1).json`（ShareGPT格式，模拟图谱查询交互）
核心架构	LoRA微调 × 2 + Neo4j 图检索+向量检索（统一存储）
提示词框架	RACE框架（专家视角，用于第二次LoRA微调及推理时Prompt构建）
整体架构总览
用户输入
    ↓
┌──────────────────────────────────────────────────────────────────┐
│ 感知层 (Perception)                                              │
│  实体链接 → 意图识别 → 问题解析                                  │
│  输出: {实体列表, 意图类型, 查询要素}                            │
└──────────────────────────┬───────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────────┐
│ 规划层 (Planning)                                                │
│  步骤1: 检索策略选择（按意图决定图/向量路径）                     │
│  步骤2: 多跳查询规划（复杂问题自动扩展）                          │
│  步骤3: 结果融合（去重/排序/格式化）                              │
│  步骤4: RACE框架构建完整Prompt（关键环节）                       │
│    R(角色) + A(行动) + E(期望) → system消息（固定模板）          │
│    C(情境/参考信息) → user消息前缀（由步骤3动态填充）             │
│    用户原始问题 → user消息主体                                    │
└──────────────────────────┬───────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────────┐
│ 工具层 (Tools) ←──── 记忆层 (Memory)                             │
│  图查询工具(Cypher)  ←┐                                          │
│  向量检索工具(Cypher) ←┼─ Neo4j 统一存储                          │
│                      ←┘  （结构化数据 + 向量索引）                 │
│  工具调用机制(function calling)                                   │
└──────────────────────────┬───────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────────┐
│ 模型层 (Model)                                                   │
│  Qwen2.5-7B-Instruct + QA_LoRA（第二阶段微调）                   │
│  接收RACE格式的完整Prompt → 生成带来源标注的答案                 │
└──────────────────────────┬───────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────────┐
│ 反馈层 (Feedback)                                                 │
│  测试集评估 → 对比实验（纯LLM/纯向量/纯图/完整版）→ 难例迭代         │
└──────────────────────────────────────────────────────────────────┘

第一步：模型 (Model)
1.1 下载 Qwen2.5-7B-Instruct 到本地
项目	内容
做什么	将模型权重文件下载到本机磁盘，验证加载和推理正常
如何实现	① 确认硬件：GPU显存≥16GB（全精度）或≥8GB（4-bit量化），磁盘≥15GB<br>② 使用 `huggingface-cli` 或 `modelscope` 下载 `Qwen/Qwen2.5-7B-Instruct`，保存到 `models/Qwen2.5-7B-Instruct/`<br>③ 编写验证脚本：加载 model 和 tokenizer，输入测试文本，确认 generate() 正常产出中文回复<br>④ 确认 ChatML 模板正常：`tokenizer.apply_chat_template(messages, tokenize=False)`
1.2 第一阶段 LoRA 微调——三元组抽取
项目	内容
目标	让 Qwen 学会从辑佚史文本中抽取实体和关系三元组，用于离线扩充知识图谱
何时使用	智能体运行时不加载此LoRA。仅在数据构建阶段，对新文本批量抽取三元组以扩充图谱
如何实现	①构建训练数据： 
- 遍历 data.json 的 entities，将 text + properties 拼接为一段"文献描述文本" 
- 将该实体与其他实体的关系整理为三元组：`(实体A, 关系类型, 实体B)` 
 - 构造 200-500 条指令对：instruction = "请从以下文本中抽取辑佚史实体和关系三元组：
{文本}"，output = "{三元组列表}"
②配置与训练：
- PEFT LoRA: rank=16, alpha=32, dropout=0.05, target_modules=["q_proj","v_proj","k_proj","o_proj"] 
4-bit QLoRA 加载，lr=2e-4, batch_size=4, epochs=3, max_length=1024  保存到 `lora_checkpoints/extraction_lora/`
③ 评估：在测试集上计算精确率/召回率/F1
1.3 第二阶段 LoRA 微调——问答生成（RACE框架驱动）
智能体运行时加载的 LoRA。本阶段核心是将RACE框架融入训练数据的构造中——训练样本的输入侧就是 RACE 格式，让模型在微调阶段"内化"这套输出规范。
项目	内容
目标	让 Qwen 学会基于检索到的"证据上下文"生成专业、有来源标注的辑佚史答案
与RACE框架的关系	第二阶段微调的每一条训练数据，其输入格式都是按照RACE框架构造的：
- system消息 = R(角色) + A(行动) + E(期望)（固定模板）
- user消息 = 【参考信息】{检索上下文} + 【用户问题】{问题}（动态填充）
- assistant消息 = 标准答案（带来源标注）
这样训练出来的模型，在推理时即使不依赖外部模板，也能按RACE规范输出
操作步骤：
步骤1：清洗 sft_alpaca(1).json
- 现有 134 条数据，output 是占位符"资料中有相关记载"
- 对每条 instruction，找到 data.json 中对应的实体，提取真实属性回填为真实答案
- 每条答案末尾补充 `（来源：{实体名称}）`
- 示例：
- 原始：instruction=“关于 全宋文，你知道哪些内容？”，output=“资料中有相关记载”
- 回填后：output=“全宋文是一部诗文总集，内容涵盖宋代各体文章。（来源：全宋文实体记录）”
步骤2：按RACE框架构造训练数据格式（核心）
将每条数据转换为 Qwen 的 ChatML 格式，system 和 user 消息按 RACE 构造：
训练数据格式（单条）：

[
  {
    "role": "system",
    "content": "【R-角色】你是一位专精于辑佚学与中国古典文献学的AI助教。你的知识体系涵盖：唐宋类书辑佚、清代辑佚学史、辑佚方法论（校勘、辨伪、考证）、辑佚流派与学术传承。\n\n【A-行动】请基于以下【参考信息】回答用户的问题。执行步骤：1.理解问题意图 2.在参考信息中定位相关证据 3.组织为严谨的学术回答 4.标注每条信息的出处。如果参考信息不足以完整回答，明确说明哪些部分有据可查、哪些部分存疑。\n\n【E-期望】请遵循以下输出规范：\n1.答案结构：先给出直接结论，再展开论据说明\n2.来源标注：每条关键信息后以（来源：{实体名称/关系描述}）格式标注出处\n3.学术用语：使用"据记载"、"据考证"、"推测"等分层级的确信度表述\n4.不确定性处理：参考信息不足时，如实说明而非编造\n5.语言风格：采用学术中文，简洁准确"
  },
  {
    "role": "user",
    "content": "【参考信息】\n{从data.json中提取的实体属性文本、关系描述}\n\n【用户问题】\n{原始instruction}"
  },
  {
    "role": "assistant",
    "content": "{真实答案，带来源标注}"
  }
]
关键说明：
- R(角色) + A(行动) + E(期望) 三段是**固定模板**，所有训练样本共用
- C(情境/参考信息) 是**动态填充**的，每条样本不同，来源于 data.json 中对应实体的属性
- 这样构造后，模型在训练时看到的每条输入都是"先看到 RACE 规范，再看到检索上下文，最后回答问题"
步骤3：利用 sharegpt_toolcall(1).json
- 该文件中的 function_call → observation → gpt 交互链条，可以平铺为 RACE 格式
- function_call 的 arguments 和 observation 的 results → 填入 C 段的【参考信息】
- gpt 回复 → 作为 assistant 的标准答案
- tools 定义 → 用于训练模型的 function calling 能力（可选增强）
步骤4：数据增强
基于 data.json 全部实体，用模板再生成 400-800 条 RACE 格式问答，覆盖四种类型：
类型	数量目标	示例	C段填充来源
事实问答	200	"《玉函山房辑佚书》有多少卷？"	对应实体的properties
关系问答	200	"王仁俊和马国翰有什么关系？"	对应关系的description
脉络问答	100	"清代辑佚学经历了哪几个阶段？"	Time标签实体的属性
方法问答	100	"辑佚的三原则是什么？"	Methodology标签实体的属性
每条增强数据的输入侧都按步骤2的 RACE 格式构造。
步骤5：训练配置
- 从原始 Qwen2.5-7B-Instruct 加载（不加载第一阶段 LoRA）
- LoRA: rank=16, alpha=32, target_modules=["q_proj","v_proj","k_proj","o_proj"]
- 训练: lr=1e-4, epochs=5, batch_size=4（根据显存调整）
- 保存到 `lora_checkpoints/qa_lora/`
步骤6：训练完成后验证
- 用 RACE 格式构造测试 prompt，输入模型，检查输出是否包含来源标注
- 对比微调前后的输出质量差异
1.4 推理接口封装
项目	内容
做什么	编写模型加载和生成函数，供智能体调用
如何实现	①`model_loader.py`：启动时加载原始 Qwen + QA_LoRA，保持常驻
②`generator.py`：接收完整的 messages 列表（system+user），调用 model.generate()
③生成参数：temperature=0.5, top_p=0.9, max_new_tokens=1024, repetition_penalty=1.05

第二步：记忆 (Memory)
2.1 从 data.json 构建知识图谱（结构化记忆）
项目	内容
目标	将 data.json 的 entities 和 relationships 导入 Neo4j
如何实现	①安装启动 Neo4j（Docker: `docker run -p 7474:7474 -p 7687:7687 neo4j`）② 遍历 entities 创建节点：`CREATE (e:Entity {id:$id, label:$label, name:$text}) SET e += $properties`
③ 遍历 relationships 创建关系：`MATCH (a {id:$source}),(b {id:$target}) CREATE (a)-[:RELATES {type:$type}]->(b)`
④ 建立索引：`CREATE INDEX FOR (e:Entity) ON (e.name)`
⑤ 导出实体词典供感知层使用
注意事项	data.json 中可能有 source 或 target 不存在的无效关系，需先过滤
2.2 构建 Neo4j 向量索引（语义记忆）
项目	内容
目标	将 data.json 中的文本描述向量化存入 Neo4j，在图数据库内直接支持语义检索
为什么不用独立向量库	本项目数据量在千级别，Neo4j 5.x 原生向量索引完全够用，减少一个外部依赖，架构更简洁
如何实现	①下载中文 embedding 模型：`BAAI/bge-large-zh-v1.5`（1024维）或 `text2vec-base-chinese`（768维）
②文本分块：每个实体的 properties 文本 + 每个关系的 description → 独立文本块，标注来源 id
③ 在 Neo4j 中为每个 Entity 节点的 `embedding` 属性注入向量（调用 embedding 模型生成）
④ 建向量索引：
`CREATE VECTOR INDEX entity_embedding_index FOR (e:Entity) ON e.embedding OPTIONS {indexConfig: {vector.dimensions: 768, vector.similarity_function: 'cosine'}}`
⑤ 语义检索直接走 Cypher：
`CALL db.index.vector.queryNodes('entity_embedding_index', 5, $query_embedding) YIELD node, score RETURN node.name, node.properties, score`
2.3 对话历史管理（短期记忆）
项目	内容
目标	支持多轮对话上下文连贯
如何实现	内存维护 `history = [{"role":"user","content":"..."}, {"role":"assistant","content":"..."}]`，窗口上限6轮或2048tokens

第三步：感知 (Perception)
3.1 实体链接
项目	内容
目标	从用户问句中识别知识图谱中的实体
如何实现	①加载实体词典（从Neo4j导出）+ 别名表
②jieba分词，提取名词短语，与实体词典做编辑距离≤2的模糊匹配
③补充语义匹配：问句向量与实体名称向量计算余弦相似度，取top-3
④兜底：调用第一阶段LoRA实时抽取
⑤ 输出：`[{"name":"马国翰","id":"S_MA_GUOHAN","confidence":0.95}]`
3.2 意图识别
项目	内容
做什么	判断问句类型，决定检索策略
如何实现	规则法（先做）：
- FACTUAL：含"谁/什么/多少/哪/是什么"
- RELATION：含"关系/关联/联系/与…有什么"
- CHAIN：含"发展/历程/演变/脉络/阶段"
- METHOD：含"方法/原则/如何/步骤/程序"
- COMPARE：含"区别/对比/比较/谁更/异同"
匹配不到默认 FACTUAL

3.3 问题解析
项目	内容
目标	提取检索所需的结构化要素
如何实现	RELATION用正则提取A和B；CHAIN提取主题和时间范围；COMPARE提取比较对象和比较维度。输出：`{"intent":"RELATION","entities":["马国翰","王仁俊"],"parsed":{...}}`

第四步：工具 (Tools)
4.1 图查询工具
函数	功能	Cypher
`query_entity_by_name(name)`	查实体属性	`MATCH (e {name:$name}) RETURN e`
`query_entity_relations(id)`	查一跳关系	`MATCH (e {id:$id})-[r]-(n) RETURN r, n`
`query_relation_between(a,b)`	查实体间路径	`shortestPath((a)-[*..4]-(b))`
`query_by_label(label)`	按类型查列表	`MATCH (e {label:$label}) RETURN e`
结果统一格式化为自然语言描述：`马国翰（辑佚者）→ compiledBy → 玉函山房辑佚书（辑本，594卷）`
4.2 向量检索工具（基于Neo4j）
项目	内容
目标	问句语义相似度搜索（在图数据库内完成，无需外部向量库）
如何实现	将用户问句用 embedding 模型向量化 → 调用 Neo4j 向量索引查询：
`CALL db.index.vector.queryNodes('entity_embedding_index', $k, $query_vec) YIELD node, score`
→ 返回 `[{"name":"马国翰","label":"Person","properties":{...},"score":0.85}]`


4.3 工具调用机制（function calling）
- 定义工具 Schema（`kg_find_entities`、`kg_get_entity_fields` 等），注入 system prompt
- 模型输出 function_call → 解析执行 → 结果返回给模型 → 模型生成最终答案
- sharegpt_toolcall(1).json 即此模式的训练数据

第五步：规划 (Planning)
5.1 检索策略选择
意图	检索路径	说明
FACTUAL	图查询优先	直接从KG获取实体属性，图无结果再走向量
RELATION	图查询优先	查实体间路径，无路径时向量补充
CHAIN	图查询 + 向量检索并行	按时间标签展开，同时补充文本描述
METHOD	向量检索 + 图查询并行	先找方法定义，再找相关案例
COMPARE	图查询(分别查两者) + 向量检索	需要更丰富的上下文
5.2 多跳查询
- 从感知层给出的实体出发，每次查询一跳
- 判断结果是否满足问题需求，不够则取新实体继续下一跳
- 最多3跳，遇到环路停止
5.3 结果融合
- 去重：图检索和向量检索可能指向同一实体，按 id 去重
- 排序：图检索（精确匹配）优先于向量检索（语义匹配），同类型按 score 降序
- 截断：总 token 数超过模型窗口限制时，按 score 从低到高移除
- 格式化为一段自然语言【参考信息】，填入 RACE 的 C 段
5.4 RACE Prompt 构建
在工具层完成检索并填充了 C 段内容后，规划层将 R+A+C+E 四段组装为完整 Prompt。
组装逻辑：
[system]
【R-角色】
你是一位专精于辑佚学与中国古典文献学的AI助教。你的知识体系涵盖：
唐宋类书辑佚、清代辑佚学史、辑佚方法论（校勘、辨伪、考证）、
辑佚流派与学术传承。

【A-行动】
请基于以下【参考信息】回答用户的问题。执行步骤：
1. 理解问题意图
2. 在参考信息中定位相关证据
3. 组织为严谨的学术回答
4. 标注每条信息的出处。
如果参考信息不足以完整回答，明确说明哪些部分有据可查、哪些部分存疑。

【E-期望】
请遵循以下输出规范：
1. 答案结构：先给出直接结论，再展开论据说明
2. 来源标注：每条关键信息后以（来源：{实体名称/关系描述}）格式标注出处
3. 学术用语：使用"据记载"、"据考证"、"推测"等分层级的确信度表述
4. 不确定性处理：参考信息不足时，如实说明而非编造
5. 语言风格：采用学术中文，简洁准确

[user]
【参考信息】
{Neo4j图检索结果，格式化为自然语言段落}
{Neo4j向量检索结果，格式化为自然语言段落}

【用户问题】
{用户的原始问题}
关键说明：
- R+A+E 三段 = 固定模板，在 system 消息中，微调时和推理时完全一致
- C 段 = 动态填充，在 user 消息中作为前缀，由 5.3 结果融合模块提供
- 用户原始问题 = user 消息的主体
- 这个格式与第二阶段 LoRA 训练数据的格式完全一致，确保训练和推理的分布对齐
5.5 后处理
- 检查模型输出是否包含来源标注，如缺失则补充
- 如输出为空或明显错误，返回"抱歉，根据现有参考信息无法回答"

第六步：反馈 (Feedback)
6.1 评估测试集
人工编写 50-80 题，覆盖所有意图类型，每题附带正确答案和预期检索路径。
6.2 对比实验
四种配置对比：
配置	检索方式	用途
A-纯LLM	无检索，纯模型回答	基线
B-纯图（结构化）	仅 Neo4j Cypher 图查询	消融
C-纯向量（语义）	仅 Neo4j 向量索引查询	消融
D-Graph+Vector	图查询 + 向量检索（均走 Neo4j）	完整系统
6.3 人工评分
每个配置随机抽 20 条，从正确性、完整性、来源追溯、专业性四个维度打分（1-5）。
6.4 迭代优化
收集失败案例 → 分析原因（实体链接/检索覆盖/模型理解）→ 针对性改进 → 每收集 50 条难例重训一轮。

构建顺序表
顺序	模块	步骤	依赖	预计耗时
1	模型	1.1 下载Qwen	无	1h
2	记忆	2.1 构建知识图谱	无	2天
3	模型	1.2 第一阶段LoRA（三元组）	步骤1	3天
4	记忆	2.2 构建Neo4j向量索引	步骤2	0.5天
5	感知	3.1-3.3 实体链接+意图+解析	步骤2（需实体词典）	2天
6	工具	4.1-4.3 查询+检索+调用机制	步骤2+4	1天
7	规划	5.1-5.5 策略+多跳+融合+RACE	步骤5+6	2天
8	模型	1.3 第二阶段LoRA（问答+RACE）	步骤7（RACE设计完成）+步骤1	3-5天
9	模型	1.4 推理接口封装	步骤8	0.5天
10	反馈	6.1-6.4 评估+对比+迭代	全部完成	3天
11	前端	Web界面	步骤9	3天
建议推进路线：1 → 2（与3并行）→ 5 → 6 → 7 → 8 → 10 → 11

分工说明
角色
角色	负责人	主要职责
A（模型与感知）	A	模型下载与微调、感知层（实体链接/意图/解析）、推理接口
B（数据与架构）	B	知识图谱（含向量索引）、工具层、规划层、前端
//根据需要协作开发

阶段一：基础环境搭建（第1-2天）


成员	任务	DDL
A	步骤1：下载 Qwen2.5-7B-Instruct，完成验证脚本	第2天 （5月15日
B	步骤2：安装 Neo4j，构建知识图谱，导入全部实体与关系	第2天 （5月15日

阶段二：模型训练 × 向量索引（第3-5天，并行）
成员	任务	DDL
A	步骤3：第一阶段 LoRA 微调（三元组抽取），含训练数据构建、训练、评估	第5天  （5月17日
B	步骤4：下载 embedding 模型，在 Neo4j 中构建向量索引（实体文本 → 向量 → 写入节点 embedding 属性）	第5天   （5月17日

阶段三：感知层 × 工具层（第6-8天，并行）
成员	任务	DDL
A	步骤5：感知层开发（实体链接 3.1 + 意图识别 3.2 + 问题解析 3.3）	第8天    （5月20日
B	步骤6+7：工具层开发（图查询 4.1 + 向量检索 4.2 + function calling 4.3）+ 规划层（检索策略 5.1 + 多跳查询 5.2 + 结果融合 5.3 + RACE Prompt 构建 5.4 + 后处理 5.5）	第8天    （5月20日
A 依赖 B 在阶段二产出的 Neo4j 实体词典；B 在阶段二完成后产出 JSON 格式的实体词典供 A 使用。建议 B 在第5天将实体词典文件放到共享目录。

阶段四：核心微调 × 前端（第9-13天，并行）
成员	任务	DDL
A、B	步骤8：第二阶段 LoRA 微调（问答+RACE），含数据清洗/回填、RACE格式构造、数据增强、训练、验证	第13天  (5月25日
B	步骤11： 前端对接	第13天   （5月25日
A 依赖 B 在阶段三完成的 RACE Prompt 构建模块的设计结论，用于确定训练数据格式的最终版本。建议 B 在第8天将 RACE 模板的确定版本同步到共享目录。



阶段五：联调与评估（第14-17天，协作）
成员	任务	DDL
A	步骤9：推理接口封装 + 步骤10：评估测试集编写、对比实验	第17天  （待定
B	步骤10：对比实验执行、前端联调对接、步骤9配合测试	第17天   （待定
本阶段前后端联调，建议两人同时在线。A 编写 50-80 题测试集，B 负责运行四种配置的对比实验并记录结果。
关键依赖与交付物
时间节点	交付方	交付物	用途
第2天	B → A	Neo4j 实体词典（JSON）	A 开发感知层使用
第5天	A → B	第一阶段 LoRA 权重 + 评估报告	确认三元组抽取可用
第8天	B → A	RACE Prompt 模板确定版 + Neo4j 向量索引用法文档	A 用于第二阶段微调的数据格式对齐
第13天	A → B	第二阶段 LoRA 权重 + 推理接口	B 用于前端联调

项目文件统一方案
建立统一的共享工作区，确保代码、数据、模型权重等文件保持同步。
方案：共享组目录 + Git 版本控制
//备选方案：各自建立相同结构目录，统一文件
1. 建立共享目录
在服务器上创建共享目录，使两人均具有读写权限：
# 管理员或已有 sudo 权限者执行
sudo mkdir -p /data/jys-agent
sudo groupadd jys-agent
sudo usermod -a -G jys-agent 成员A账号
sudo usermod -a -G jys-agent 成员B账号
sudo chown -R :jys-agent /data/jys-agent
sudo chmod -R 775 /data/jys-agent
sudo chmod g+s /data/jys-agent   # 使新建文件自动继承组
两人重新登录后生效。
2. 目录结构约定
/data/jys-agent/              ← 共享根目录（两人均有读写权限）
├── models/                   ← 模型权重（A 维护）
│   └── Qwen2.5-7B-Instruct/
├── lora_checkpoints/         ← LoRA 权重（A 维护）
│   ├── extraction_lora/
│   └── qa_lora/
├── data/                     ← 数据文件（两人共用）
│   ├── data.json
│   ├── sft_alpaca(1).json
│   ├── sharegpt_toolcall(1).json
│   ├── entity_dict.json      ← 实体词典（B 产出，A 使用）
│   └── test_set.json         ← 测试集（A 产出，B 使用）
├── embeddings/               ← Embedding 模型（B 维护）
│   └── bge-large-zh-v1.5/
├── src/                      ← 源代码（Git 管理，两人共同维护）
│   ├── model/                ← 模型加载、推理（A 主要负责）
│   ├── memory/               ← 图谱构建、向量索引（B 主要负责）
│   ├── perception/           ← 实体链接、意图、解析（A 主要负责）
│   ├── tools/                ← 图查询、向量检索（B 主要负责）
│   ├── planning/             ← RACE 构建、策略、融合（B 主要负责）
│   ├── frontend/             ← Web 前端（B 主要负责）
│   └── evaluation/           ← 评估脚本、对比实验（两人共同）
└── README.md                 ← 环境说明、启动步骤
3. Git 管理源代码
`/data/jys-agent/src/` 目录初始化为 Git 仓库，两人各自 clone 或在同一仓库中协作：
cd /data/jys-agent
git init
echo "*.pyc" > .gitignore
echo "__pycache__/" >> .gitignore
git add -A
git commit -m "init: project scaffold"
协作策略：
方式	适用场景
同一仓库，不同分支	推荐。两人在 `/data/jys-agent/src/` 下各自建分支（如 `dev-a`、`dev-b`），互不干扰，阶段性合并
`git clone` 到各自 home	如果想隔离开发环境，两人各自 clone 到自己 `~/` 下开发，公共仓库仅用于合并交付
分支策略（推荐）：
main              ← 稳定版本，仅合并经过验证的代码
├── dev-a         ← 成员A 的开发分支
└── dev-b         ← 成员B 的开发分支
4. 大文件处理
模型权重（.bin/.safetensors）等大文件不入 Git，存储于共享目录下，通过路径约定（`/data/jys-agent/models/`）直接访问，避免 Git LFS 的额外复杂度。
建议在 `.gitignore` 中添加：
models/
lora_checkpoints/
embeddings/
data/
*.bin
*.safetensors
*.pth
5. 每日同步检查清单
动作	时机	说明
`git pull` 对方分支的最新代码	每天开始工作前	了解对方最新改动，避免冲突
交付物放到共享目录	完成阶段性产出时	对方可直接使用，无需额外协调
合并到 `main` 分支	阶段性联调通过后	两人共同确认
以用户问"马国翰和王仁俊有什么关系？"为例：
训练阶段（第二阶段LoRA）
训练集中的一条数据示例如下（ChatML格式）：
system:
【R-角色】你是一位专精于辑佚学与中国古典文献学的AI助教。你的知识体系涵盖：唐宋类书辑佚、清代辑佚学史、辑佚方法论（校勘、辨伪、考证）、辑佚流派与学术传承。
【A-行动】请基于以下【参考信息】回答用户的问题。执行步骤：1.理解问题意图 2.在参考信息中定位相关证据 3.组织为严谨的学术回答 4.标注每条信息的出处。如果参考信息不足以完整回答，明确说明哪些部分有据可查、哪些部分存疑。
【E-期望】请遵循以下输出规范：1.先给结论再展开 2.标注来源 3.使用学术用语 4.信息不足如实说明 5.简洁准确

user:
【参考信息】
马国翰（辑佚者），辑有《玉函山房辑佚书》594卷，清代道光年间人。
王仁俊（辑佚者），辑有《经籍佚文》，工作在马国翰基础上续编。
关系：王仁俊与马国翰之间存在学术传承关系，王仁俊的工作是马国翰基础上的续编与补编。

【用户问题】
马国翰和王仁俊有什么关系？

assistant:
马国翰与王仁俊之间存在学术传承关系。王仁俊的辑佚工作是在马国翰《玉函山房辑佚书》基础上的续编与补编。（来源：关系 evolvesFrom 的定义记录）
推理阶段
推理时的 Prompt 结构与训练时完全一致：
[system] ← R+A+E 固定模板（与训练时相同）
[user] ← 【参考信息】{检索结果} + 【用户问题】{用户提问}
模型在训练时已经学会了："看到 RACE 格式的输入 → 检索上下文在【参考信息】中 → 按 E 规范输出 → 标注来源"。因此推理时直接按此格式输入即可得到符合规范的答案。
