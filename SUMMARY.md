# 辑佚史智能体 — 构建总结

## 一、整体架构

用户提问 → 感知层 → 规划层 → 工具层(检索) → 模型生成 → 答案

```
用户: "马国翰和王仁俊有什么关系？"
  ↓
感知层: 实体=[马国翰, 王仁俊]  意图=RELATION   
--jieba 分词 + 实体链接（精确/模糊匹配）+ 意图识别（5 种：事实/关系/脉络/方法/比较）
  ↓
规划层: 决策走图查询 + 多跳扩展  
  ↓
工具层: Neo4j Cypher 查路径 → 马国翰 → 王仁俊
        Neo4j 向量索引 → 语义补充
  ↓
模型层: RACE Prompt 组装检索结果 → Qwen2.5-7B 生成答案
  ↓
答案: "马国翰与王仁俊存在学术传承关系。王仁俊在马国翰
       《玉函山房辑佚书》基础上续编与补编..."
```



## 二、怎么做到"检索 + 生成"

### 2.1 两套检索，一个数据库

全部存在 Neo4j 里，不需要外部向量库：

| 检索方式 | 做什么 | 举例 |
|---|---|---|
| **图查询 (Cypher)** | 精确查实体属性、查关系路径、多跳扩展 | `MATCH (马国翰)-[r]-(王仁俊)` |
| **向量检索 (语义)** | 问句和实体描述做相似度匹配 | "清代山东籍辑佚学者" → 找到马国翰 |

### 2.2 多跳查询

从用户问句中识别的实体出发，自动扩展关联网络：
- 第 1 跳：查马国翰 → 找到 清代、玉函山房辑佚书、王仁俊
- 第 2 跳：查王仁俊 → 找到 经籍佚文、经史子辑佚成果对照表
- 每跳最多扩展 3 个邻居，最多 2 跳，自动去重

### 2.3 RACE 提示词框架

检索结果组织成"参考信息"，和用户问题一起喂给模型：

```
[System]  R-角色: 辑佚学AI助教
          A-行动: 基于参考信息回答，标注来源
          E-期望: 先结论后展开，学术中文

[User]    【参考信息】
          {Neo4j图检索 + 向量检索结果}
          
          【用户问题】
          马国翰和王仁俊有什么关系？
```

## 三、构建流程

### 阶段一：数据与知识图谱

1. **数据准备**：208 个辑佚史实体（辑本、辑佚者、历史时期、研究方法、学术流派）+ 169 条关系
2. **Neo4j 知识图谱**：实体 → 节点，关系 → 边，建普通索引和向量索引
3. **向量化**：用 bge-large-zh-v1.5（1024维）把所有实体描述嵌入到 Neo4j

### 阶段二：感知与检索

4. **感知层**：jieba 分词 + 实体链接（精确/模糊匹配）+ 意图识别（5 种：事实/关系/脉络/方法/比较）
按关键词判断五种意图：

问句含"区别/对比/不同"     → COMPARE
问句含"关系/关联/联系"     → RELATION
问句含"发展/脉络/阶段"     → CHAIN
问句含"方法/原则/如何"     → METHOD
问句含"谁/什么/多少/哪"    → FACTUAL
都匹配不到               → 默认 FACTUAL

5. **工具层**：图查询（Cypher）+ 向量检索 + function calling
6. 
7. **规划层**：按意图选检索策略，多跳查询，结果去重排序，组装 RACE Prompt

单轮记忆，每次请求独立处理


### 阶段三：推理与部署

7. **模型**：Qwen2.5-7B-Instruct (bf16)，RTX 5090 32GB，推理时不做 LoRA
8. **API 服务**：FastAPI 包装全链路（感知→检索→生成），POST /chat
9. **前端**：React + Vite，对话界面直接调 API

## 四、技术栈

| 层 | 技术 |
|---|---|
| 模型 | Qwen2.5-7B-Instruct + PyTorch (bf16) |
| 图谱 | Neo4j 5.26.5 (Cypher + 向量索引) |
| Embedding | bge-large-zh-v1.5 (1024维) |
| API | FastAPI + uvicorn |
| 前端 | React 18 + Vite 5 |
| 部署 | tmux 后台 + SSH 隧道 |

